# سیستم طراحی رُکاد‌اسکول — DESIGN.md

> مرجع توکن‌های این ریپو. استخراج‌شده از Figma (`rokadschool.ir` — صفحه Design System، node `1:2`)
> و **هم‌راستا با `tailwind.config.js`**. اگر مقداری در کد با این جدول نخواند، یا کد را اصلاح کنید یا همین فایل را (با تأیید تیم طراحی).

## ۱. پنج شخصیت برند (Persona Themes)

هر تم یک طیف کامل Light → Normal → Dark → Darker با حالت‌های hover/active دارد.

| تم | کلید Tailwind | Normal (Hex) | کاربرد |
| --- | --- | --- | --- |
| 🌿 اکوسیستم (Primary) | `teal` / `teal-alt` | `#59BBAF` | رنگ اصلی برند، دکمه‌ها، لینک فعال |
| 🔵 پسر (Male) | `navy` / `navy-alt` | `#202A5A` | متون، دکمه ثانویه، تم پسرانه |
| 🌸 دختر (Female) | `magenta` | `#E0195B` | تم دخترانه، بج‌ها |
| 🟠 کالج (College) | `orange` / `orange-alt` | `#F8A41D` | کالج، آیکون‌های تأکیدی |
| 🟣 کلوپ (Club) | `purple` | `#652D90` | کلوپ‌ها، کارت‌های ویژه |

متن/خنثی: `ink` `#292827`، `ink-dark` `#1F1E1D`، `ink-darker` `#0E0E0E`.

### طیف کامل (نمونه — Female و Ecosystem)

| | Light | Normal | Dark | Darker |
| --- | --- | --- | --- | --- |
| Ecosystem | `#EEF8F7` | `#59BBAF` | `#438C83` | `#1F413D` |
| Female | `#FCE8EF` | `#E0195B` | `#A81344` | `#4E0920` |
| Male | `#E9EAEF` | `#202A5A` | `#182044` | `#0B0F1F` |
| College | `#FEF6E8` | `#F8A41D` | `#BA7B16` | `#57390A` |
| Club | `#F0EAF4` | `#652D90` | `#4C226C` | `#231032` |

 aliases متنی: `teal-text` `#347E75` (متن teal روی زمینه روشن)، `magenta-text` `#CA1752` (حالت hover دخترانه طبق طیف Figma).

## ۲. پس‌زمینه‌ها

| توکن | مقدار | کاربرد |
| --- | --- | --- |
| `bg-mint` | `#F2FAF9` | پس‌زمینه اکوسیستم/کپسول هدر |
| `bg-blush` | `#FEFAFB` | پس‌زمینه دخترانه |
| `bg-lavender` | `#F4F5FB` | پس‌زمینه پسرانه/خنثی سرد |
| `bg-neutral` | `#F6F6F6` | خنثی (سکشن‌های About، RokadHierarchy) |

## ۳. تایپوگرافی

- فونت: **IRANSansX** (fallback: Tahoma) — `font-iransans`
- مقیاس فارسی (size/line-height، نسبت D4): `xs 10/15` · `sm 13/19.5` · `base 16/24` · `md 20/30` · `lg 25/37.5` · `xl 39/58.5` · `2xl 49/73.5` · `3xl 61/91.5`
- تراز همیشه `dir="rtl"`؛ اعداد فارسی در متن نمایشی.

## ۴. فرم و سایه

- گردی‌ها: `xs 5px` · `sm 8px` · `md 12px` · `lg 17px` · `xl 24px` · `2xl 34px` · `pill 40px` + `corner-shape: squircle` روی بج/کارت/دکمه.
- **سایه سخت برند** (فقط المان تأکیدی): `2.75px 2.75px 0 <theme>`؛ عمق معمولی = حاشیه/سایه نرم `shadow-soft`.
- لبه‌های مورب (tinted corners): الگوی `rounded-[0_0.75rem_0_0.75rem]` برای کارت‌ها/دکمه‌های شخصیت‌دار.

## ۵. قوانین استفاده (از دیزاین‌سیستم)

1. **همیشه توکن، نه هگز سخت.** مقادیر داخل کلاس‌های arbitrary (`bg-[#...]`) فقط وقتی مجازند که توکن معادل هنوز ساخته نشده — و سپس باید توکن‌سازی شوند.
2. انتخاب تم بر اساس بخش: اکوسیستم=عمومی/برند، پسر/دختر=محتوای جنسیت‌محور، کالج=دانشگاهی، کلوپ=انجمن‌ها.
3. کنتراست متن: متن روی رنگ Normal همیشه `#FFFFFF`؛ متن عادی `ink` روی `light`/سفید.
4. پترن استاندارد پس‌زمینه سکشن‌ها: `/assets/Pattern/layout-pattern.png` با `opacity-30` (ابداع پترن جدید ممنوع).
5. ارتفاع سکشن‌ها `≤ 100vh` (استثنا: تایم‌لاین Story در /about).
6. هدر: کپسول `max-w-[75rem]`/`radius 38px` فقط بالای صفحه؛ بعد از **50vh** اسکرول، نوار فول‌عرض چسبان تا انتهای صفحه.

## ۶. فاصله‌ها

- Container مشترک: `max-w-[75rem]` با `px-4 sm:px-6 lg:px-8` (`src/layout/Container.jsx`).
- پدینگ عمودی سکشن دسکتاپ: `2.5rem–4.5rem` (سقف 100vh را نشکنید).

## ۷. تغییرات اعمال‌شده در باگ‌آودیوت (۲۰۲۶-۰۹ — هم‌راستاسازی کد با همین سند)

| قبل (کد) | بعد (طبق Figma) | توکن |
| --- | --- | --- |
| `#58BDAF` | `#59BBAF` | teal |
| `#21295A` | `#202A5A` | navy |
| `#F4971F` / `#F9A21D` | `#F8A41D` | orange |
| `#4F215A` | `#652D90` | purple (Club normal) |
| `#CE1754` | `#CA1752` | magenta-text (hover Female) |

~۱۴۰ هگز در ۲۳ فایل؛ shadowهای `rgba(33,41,90,…)→rgba(32,42,90,…)` و `rgba(88,189,175,…)→rgba(89,187,175,…)` نیز بازنویسی شدند.

## ۸. منابع

- Figma: `rokadschool.ir`، key `cblkXUCIFtCkDmNOLq9dHz`، صفحه Design System node `1:2`
- سند کامل طیف‌ها: `Rokad-design-system.md` (۴۴۵ خط، ۶۵ توکن رنگی + ۸ توکن متنی)
